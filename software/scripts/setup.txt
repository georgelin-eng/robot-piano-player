pinMode(A0, INPUT); // current_sense1
pinMode(A1, INPUT); // current_sense2
pinMode(A2, INPUT); // prox_sense1
pinMode(A3, INPUT); // prox_sense2
pinMode(D24, INPUT); // ENCA
pinMode(D25, INPUT); // ENCB
pinMode(D6, OUTPUT); // PWM1
pinMode(D5, OUTPUT); // PWM2
